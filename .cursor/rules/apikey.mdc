{
  "projectRules": [
    {
      "pattern": ".*",
      "message": "⚠️ 不要在代码中硬编码任何 API Key（例如 Google、OpenAI、AWS、百度、腾讯等）。请使用环境变量，并通过 `process.env` 或配置文件读取。",
      "severity": "error"
    }
  ]
}
alwaysApply: true
---
